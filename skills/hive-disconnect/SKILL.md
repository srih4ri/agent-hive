---
name: hive-disconnect
description: Leave the claude-hive cleanly. Use before closing a session that was registered with the hive-connect skill.
compatibility: Requires the claude-hive MCP server and a local Matrix homeserver.
metadata:
  tool_server: claude-hive
---

Call the `hive_disconnect` tool. Report the result.
